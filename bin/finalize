#!/usr/bin/env bash
# bin/finalize <build-dir> <cache-dir> <deps-dir> <index>

BUILD_DIR=$1
CACHE_DIR=$2
DEPS_DIR=$3
INDEX=$4

echo "##### finalize #####"
echo "$BUILD_DIR"
echo "$CACHE_DIR"
echo "$DEPS_DIR"
echo "$INDEX"
